name: Retrain — Shrinking Stream

on:
  schedule:
    # 21:30 UTC Sunday = before Monday market open
    - cron: '30 21 * * 0'
  workflow_dispatch:
    inputs:
      universe:
        description: 'ETF universe (fi, equity, or both)'
        required: false
        default: 'both'
        type: choice
        options:
          - both
          - fi
          - equity
      force:
        description: 'Force retrain even if already ran today (true/false)'
        required: false
        default: 'false'
      benchmark:
        description: 'Run benchmark timing only (true/false)'
        required: false
        default: 'false'
      epochs:
        description: 'Max training epochs'
        required: false
        default: '80'

jobs:
  train_shrinking_fi:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    if: ${{ (github.event_name == 'schedule') || (github.event_name == 'workflow_dispatch' && (github.event.inputs.universe == 'fi' || github.event.inputs.universe == 'both')) }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - run: pip install -r requirements.txt

      - name: Check if already ran today (FI)
        if: ${{ github.event.inputs.force != 'true' && github.event.inputs.benchmark != 'true' }}
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          RESULT=$(python check_run_date.py --stream shrinking --universe fi)
          echo "Date check result: $RESULT"
          if [ "$RESULT" = "ALREADY_RAN_TODAY" ]; then
            echo "SKIP=true" >> $GITHUB_ENV
          else
            echo "SKIP=false" >> $GITHUB_ENV
          fi

      - name: Train shrinking stream (FI)
        if: ${{ env.SKIP != 'true' }}
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          if [ "${{ github.event.inputs.benchmark }}" = "true" ]; then
            echo "🔬 Benchmark mode (FI)..."
            python train.py --universe fi --benchmark --epochs ${{ github.event.inputs.epochs || '80' }}
          else
            echo "📉 Training shrinking stream (FI)..."
            python train.py --universe fi --stream shrinking --epochs ${{ github.event.inputs.epochs || '80' }}
          fi

      - name: Report completion (FI)
        if: always()
        run: |
          if [ "${{ env.SKIP }}" = "true" ]; then
            echo "⏭️  Skipped FI — already ran today"
          else
            echo "✅ Shrinking stream (FI) finished at $(date -u)"
          fi

  train_shrinking_equity:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    if: ${{ (github.event_name == 'schedule') || (github.event_name == 'workflow_dispatch' && (github.event.inputs.universe == 'equity' || github.event.inputs.universe == 'both')) }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - run: pip install -r requirements.txt

      - name: Check if already ran today (Equity)
        if: ${{ github.event.inputs.force != 'true' && github.event.inputs.benchmark != 'true' }}
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          RESULT=$(python check_run_date.py --stream shrinking --universe equity)
          echo "Date check result: $RESULT"
          if [ "$RESULT" = "ALREADY_RAN_TODAY" ]; then
            echo "SKIP=true" >> $GITHUB_ENV
          else
            echo "SKIP=false" >> $GITHUB_ENV
          fi

      - name: Train shrinking stream (Equity)
        if: ${{ env.SKIP != 'true' }}
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          if [ "${{ github.event.inputs.benchmark }}" = "true" ]; then
            echo "🔬 Benchmark mode (Equity)..."
            python train.py --universe equity --benchmark --epochs ${{ github.event.inputs.epochs || '80' }}
          else
            echo "📉 Training shrinking stream (Equity)..."
            python train.py --universe equity --stream shrinking --epochs ${{ github.event.inputs.epochs || '80' }}
          fi

      - name: Report completion (Equity)
        if: always()
        run: |
          if [ "${{ env.SKIP }}" = "true" ]; then
            echo "⏭️  Skipped Equity — already ran today"
          else
            echo "✅ Shrinking stream (Equity) finished at $(date -u)"
          fi
